import React, { Component } from 'react';
import css from '../styles/header.css';

export default class HeaderComponent extends Component {
  render() {
    return <header className="page-header">Weather App (Socket)</header>;
  }
}
